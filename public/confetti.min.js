/*! @kirilv/canvas-confetti v1.6.0 MIT */
(function () {
    var confetti = function () {
      var t = function (t, e, i) {
        return (t = Math.max(t, 0)), (e = Math.max(e, 0)), (i = Math.max(i, 0)), t / (t + e + i);
      };
      var e = function (e) {
        var i = document.createElement("canvas");
        i.setAttribute("class", "confetti-canvas");
        var n = i.getContext("2d"),
          r = [],
          o = [],
          a = e.particleCount || 100,
          s = e.colors || ["#26ccff", "#a25afd", "#ff5e7e", "#88ff5a", "#fcff42", "#ffa62d", "#ff36ff"],
          l = e.origin || { x: 0.5, y: 0.5 },
          c = e.scalar || 1;
        i.width = window.innerWidth;
        i.height = window.innerHeight;
        (i.style.position = "fixed"), (i.style.top = "0"), (i.style.left = "0"), (i.style.width = "100%"), (i.style.height = "100%"), (i.style.pointerEvents = "none"), (i.style.zIndex = "999999");
        document.body.appendChild(i);
        for (var d = 0; d < a; d++) {
          var f = Math.random() * i.width,
            h = Math.random() * i.height;
          o.push({
            x: f,
            y: h,
            angle: Math.random() * 2 * Math.PI,
            velocity: Math.random() * 5 + 2,
            radius: Math.random() * 6 + 4,
            color: s[Math.floor(Math.random() * s.length)],
            tilt: Math.random() * 10 - 10,
            tiltAngle: 0,
            tiltAngleIncrement: 0.07 * Math.random() + 0.05
          });
        }
        return (
          (function draw() {
            n.clearRect(0, 0, i.width, i.height);
            for (var t = 0; t < o.length; t++) {
              var e = o[t];
              n.beginPath();
              n.arc(e.x, e.y, e.radius * c, 0, 2 * Math.PI);
              n.fillStyle = e.color;
              n.fill();
              e.x += Math.cos(e.angle) * e.velocity;
              e.y += Math.sin(e.angle) * e.velocity + 1.5;
              e.tiltAngle += e.tiltAngleIncrement;
              e.tilt = 15 * Math.sin(e.tiltAngle);
              if (e.y > i.height || e.x < 0 || e.x > i.width) {
                o[t] = {
                  x: Math.random() * i.width,
                  y: -10,
                  angle: Math.random() * 2 * Math.PI,
                  velocity: Math.random() * 5 + 2,
                  radius: Math.random() * 6 + 4,
                  color: s[Math.floor(Math.random() * s.length)],
                  tilt: Math.random() * 10 - 10,
                  tiltAngle: 0,
                  tiltAngleIncrement: 0.07 * Math.random() + 0.05
                };
              }
            }
            requestAnimationFrame(draw);
          })(),
          {
            canvas: i,
            destroy: function () {
              i.remove();
            }
          }
        );
      };
      return { create: e };
    };
    window.confetti = confetti();
  })();
  